# 신규 에이전트 투입 및 협업 가이드 (For Multi-Track)

본 문서는 도매토피아 프로젝트에 새롭게 투입되는 AI 에이전트를 위한 가이드입니다.

## 1. 필수 확인 문서
작업 시작 전 다음 문서들을 반드시 읽고 숙지해야 합니다.
- **[RULES.md](file:///RULES.md):** 프로젝트 핵심 규칙 (호칭, 언어, 개발 철칙 등)
- **[task.md](file:///task.md):** 전체 작업 현황 및 담당 영역 확인

## 2. 작업 환경 준비 (빈 디렉토리 시작 시)
1. **레거시 소스 준비:** 프로젝트 루트(new_admin의 상위 폴더)에 `legacy_source` 폴더가 있는지 확인합니다. (PM님이 수동으로 준비해 주시는 영역입니다.)
2. **자동 세팅 실행:** `new_admin` 폴더 내의 `setup.ps1` 스크립트를 실행하여 환경 설정을 자동화합니다.
    ```powershell
    ./setup.ps1
    ```
3. **Git 최신화:** `git pull origin master`를 실행합니다.

## 3. 작업 시작 및 협업 절차
1. **브랜치 생성:** 담당한 기능에 맞는 `feature/기능명` 브랜치를 생성합니다.
2. **Task 업데이트:** `task.md`에서 본인이 담당할 항목에 에이전트 아이디를 기록하고 상태를 `[/]` (진행중)로 변경합니다.
3. **구현 계획 보고:** 작업을 시작하기 전 **PM**님께 `../legacy_source` 분석 내용과 구현 계획을 보고합니다.

## 3. 개발 규칙 (핵심 요약)
- **언어:** 한국어만 사용.
- **호칭:** 사용자를 반드시 **'PM'**님으로 지칭.
- **Parity:** 레거시 시스템과 100% 동일하게 구현 (주관적 개선 금지).
- **데이터:** 테스트 시 반드시 신규 데이터를 생성하여 사용 (실 운영 데이터 수정 금지).

## 4. 작업 완료 및 제출
1. 작업을 마치면 `task.md`를 `[x]` (완료)로 업데이트합니다.
2. 커밋 후 **PM**님께 검토를 요청합니다.
3. 승인 후 브랜치를 병합(Merge)하거나 PR을 생성합니다.
